# Kubernetes Track

V1 targets Railway/Render plus a GPU worker provider. This folder is reserved for the enterprise Kubernetes track:

- API deployment and service
- Web deployment and service
- Orchestrator deployment
- GPU worker deployment with node selectors
- Redis/PostgreSQL managed-service bindings
- Prometheus/Grafana service monitors

Do not deploy these placeholders directly.

